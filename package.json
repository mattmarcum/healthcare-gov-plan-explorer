{
  "name": "healthcare-gov-plan-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension to view and compare all HealthCare.gov marketplace plans at once.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "node build.mjs --package && cd dist && zip -r -FS ../plan-explorer.zip manifest.json content.js",
    "firefox": "web-ext run --source-dir dist --target firefox-desktop"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5",
    "web-ext": "^7.11.0"
  }
}
